# Roots Template: <%= name %>

<%= description %>

Setup
-----

- make sure [node.js](http://nodejs.org) is installed
- `npm install roots -g`
- `roots template add <%= name %>`
- `roots new project-name --<%= name %>`
- `roots watch`
- get money
